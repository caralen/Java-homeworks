package hr.fer.zemris.java.hw07.shell;

/**
 * The ShellStatus has two possible states: continue or terminate.
 * It defines if the shell should continue working or terminate the program.
 */
public enum ShellStatus {

	/** Continue working. */
	CONTINUE,
	
	/** Terminate the program. */
	TERMINATE
}
